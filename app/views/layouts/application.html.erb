<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header id="header">
      <nav>
        <div class="nav-left">
          <%= link_to "Articles", root_path %>
          <%= link_to "New Post", new_article_path %>
        </div>
        <div class="nav-right">
          <% if signed_in? %>
            <p class="user-name"><%= current_user.user_name %></p>
            <%= button_to "Logout", sign_out_path, method: :delete  %>
          <% else %>
            <%= link_to "Sign In", sign_in_path %>
            <%= link_to "Sign Up", sign_up_path %>
          <% end %>
        </div>
      </nav>
    </header>
    <section class="primary-content">
      <div id="flash">
        <% flash.each do |key, value| %>
          <div class="flash-message <%= key %>"><%= value %></div>
        <% end %>
      </div>
      <%= yield %>
    </section>
  </body>
</html>
