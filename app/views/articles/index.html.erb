<h1>Articles</h1>

<%= link_to "New Article", new_article_path %>
<ul>
  <% @articles.each do |article| %>
    <li>
      <a href="<%= article_path(article) %>">
        <h3><%= article.title %></h3>
      </a>
    </li>
  <% end %>
</ul>
